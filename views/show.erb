<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Error Or Die!</title>
  <meta name="description" content="The ERROR OR DIE!">
  <meta name="author" content="Fiverr">
  <link href="/bootstrap/css/bootstrap.min.css" media="all" rel="stylesheet" type="text/css" />
  <style>
    table {
      width: 620px;
    }

    table td, table th {
      overflow: hidden;
    }
  </style>
</head>
<body>
  <div class="container">
      <div class="navbar">
        <div class="navbar-inner">
          <a class="brand" href="#">Meshumar&copy;</a>
          <ul class="nav">
            <li class="active"><a href="#">Home</a></li>
          </ul>
        </div>
      </div>
      <h1><%= @error.id %></h1>
      <h2><%= @error.component %>#<%= @error.action %></h2>
      <h3><%= @error.error_class %></h3>
      <p class="well">
      <%= @error.env %> Environment
      </p>
      <p>
        <strong>Message:</strong><br/>
        <pre><%= @error.error_message %></pre>
      </p>
      <p>
        <strong>Params:</strong><br/>
        <pre>{<% @error.params.each_pair do |key, val| -%>
          <%= key %> => <%= val %>
        <% end -%>}</pre>
      </p>
      <p>
        <strong>Backtrace:</strong><br/>
        <pre><%= @error.backtrace %></pre>
      </p>
  </div>
  <script src="http://code.jquery.com/jquery-latest.js"></script>
  <script src="/bootstrap/js/bootstrap.min.js" type="text/javascript" />
</body>
</html>