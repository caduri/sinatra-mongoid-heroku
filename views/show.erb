<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Error Or Die!</title>
  <meta name="description" content="The ERROR OR DIE!">
  <meta name="author" content="Fiverr">
  <link href="../bootstrap/css/bootstrap.min.css" media="all" rel="stylesheet" type="text/css" />
  <style>
    table {
      width: 620px;
    }

    table td, table th {
      overflow: hidden;
    }
  </style>
</head>
<body>
  <div class="container">
      <div class="navbar">
        <div class="navbar-inner">
          <a class="brand" href="/errors">Meshumar&copy;</a>
          <ul class="nav">
            <li class="active"><a href="/errors">Home</a></li>
          </ul>
        </div>
      </div>
      <div class="page-header">
        <h1><%= @error.error_class %> <small>on <%= @error.env %> (<%= @error.host %>)</small></h1>
      </div>
      <p class="lead">
      </p>
      <dl class="dl-horizontal">
        <dt>Message</dt>
        <dd><pre><%= @error.error_message %></pre></dd>
        <dt>Environment:</dt>
        <dd><pre><%= @error.env %></pre></dd>
        <dt>URL:</dt>
        <dd><pre><%= @error.url %></pre></dd>
        <dt>Code:</dt>
        <dd><pre><%= @error.component %>#<%= @error.action %></pre></dd>
        <dt>Backtrace</dt>
        <dd><pre><%= @error.backtrace %></pre></dd>
        <dt>Params:</dt>
        <dd><pre><%= @error.params.inspect.gsub(", ", "\n") %></pre></dd>
      </dl>
  </div>
  <script src="http://code.jquery.com/jquery-latest.js"></script>
  <script src="/bootstrap/js/bootstrap.min.js" type="text/javascript" />
</body>
</html>